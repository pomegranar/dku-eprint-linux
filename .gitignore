# Build artefacts. Anchored to the repo root so packaging/debian/ is unaffected
# by the top-level debian/ that `make deb` creates.
/build/
/dist/
/debian/

__pycache__/
*.pyc
*.o
*.tmp

# Agent instruction files are personal, never publish them.
AGENTS.md
CLAUDE.md
.claude/
